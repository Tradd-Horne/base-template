version: '3.8'

services:
  web:
    build: .
    command: gunicorn basetemplate.wsgi:application --bind 0.0.0.0:8000 --workers 1 --reload
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env.local
    restart: unless-stopped
